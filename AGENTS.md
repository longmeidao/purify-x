# Purify X 项目约定

## 开始工作前

- 以 `CLAUDE.md`、`TODO.md`、`docs/Purify-X-安装说明.md` 和 `history/`
  中的会话记录恢复上下文。
- 首个 Git 基线为 v1.14.0，当前版本为 **Purify X v2.7.3**。不要从旧临时目录
  或旧 userscript 副本覆盖当前文件。
- 除非用户明确要求，不要使用 Computer Use。代码、Git 和自动化检查优先通过本地
  文件与命令完成。

## 不可破坏的行为

- 主体保持单文件 `purify-x.user.js`，不要为形式上的模块化拆散发布脚本。
- 当前登录账号和关系状态暂时无法确认的账号必须放行。已关注账号通常放行；只有
  当前帖子命中“Telegram 外链 + 推广话术”，或同时命中“限制回复 + 普通外链 +
  推广话术”时，才允许进入评分和隐藏。
- 转推卡片的作者是原推作者，转推者只在 socialContext 行里。同一套放行规则要
  作用在转推者身上；取不到转推者时必须回落到按原作者判定的既有行为。
- 引用推文的作者是相关身份，只能给外层内容提供账号名单证据；不能仅凭引用作者
  绕过外层作者的自己、已关注、永远放行或关系未知保护。已关注例外仍只限外层帖子
  自身命中上述高置信推广组合。
- 本地及订阅的“永远放行账号”优先于本地屏蔽、公开名单、社区规则、内容规则和 AI。
- 不要改名或删除现有 GM 存储键；rebrand 后也要保留升级兼容。
- 每次隐藏都必须保留可解释的评分与原因；规则调整要防止同一证据重复计分。
- 不要重新把 X 的 `cellInnerDiv` 压成零高度。连续折叠只压缩脚本自己的占位节点，
  并保留 1px 可测量尾行，避免破坏 X 虚拟列表的滚动锚点。

## 修改与验证

- 修改判定逻辑前先建立或补充对应的正常/垃圾回归样本，样本放在
  `test/fixtures/`。
- 至少运行 `npm test`（含 `node --check`、评分回归、行为信号回归和回复区
  端到端判定）；涉及存储、名单、缓存或 DOM 时，还要针对成功、失败和回收
  重挂载路径做定向检查。
- 行为判定要保持「DOM 读取」和「纯逻辑」分离：新信号写进
  `computeReplyBehaviorSignals`，DOM 抽取留在 `replyBehaviorRecords`，
  这样才能脱离浏览器回归。
- 公开说明必须区分“代码已修改”“已自动验证”“已在真实浏览器验收”和“已发布”。
- `history/` 和 `.claude/settings.local.json` 仅用于本地上下文，保持 Git 忽略。
- 提交前检查 `git diff`、未跟踪文件和敏感信息；不要提交 API Key、Cookie、私人
  名单、浏览记录或未经核实的个人数据。
