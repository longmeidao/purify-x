# 项目待办

## 1. Rebrand（已完成）

- [x] 最终品牌确定为 `Purify X`，项目目录与公开文件统一使用 `purify-x`。
- [x] 统一 userscript 的 `@name`、设置面板、菜单、提示、日志和安装文档。
- [x] 新增 `__PURIFY_X__` 公开 API，保留旧 API 作为兼容别名。
- [x] Rebrand 时保留现有 GM 存储键及兼容入口，避免升级后丢失个人名单、订阅、
  AI 设置和判定缓存。

## 2. 重构、性能整理与 GitHub 发布

- [x] 建立第一批判定回归样本（`test/`，`npm test`）：评分基线、回复区行为信号、
  端到端隐藏结果、语法与版本一致性。重构必须在这批样本全绿的前提下进行。
- [ ] 继续扩充回归样本：存储读写、名单格式与容量护栏、缓存失效、AI 安全门、
  虚拟列表回收重挂载。
- [x] 建立 `src/` 模块源码与可复现单文件构建，按配置、状态、名单同步、规则评分、
  X 数据读取、DOM/虚拟列表处理和 UI 拆分职责。
- [ ] 继续把共享闭包片段中的纯逻辑提取为可独立导入的模块，供 userscript、测试及
  未来可能的浏览器扩展入口复用。
- [ ] 合并重复的归一化、校验、缓存失效、设置读写和刷新流程，删除已废弃的兼容代码。
- [ ] 审计关键词与正则的重复计分和字段边界，保留每条屏蔽原因的可解释性。
- [x] 参考 MXGA 的数字 user ID 主键：建立 React fiber / action metadata / JSON-LD
  提取回归，再让名单按不可变 ID 优先、handle 回退匹配；冲突时必须 fail-open，不能
  用头像 URL 中的媒体 ID 冒充账号 ID。
- [x] 参考 x-algorithm 的可见性过滤结构，把页面作用域策略与证据评分分开；补充
  引用作者身份、CJK 近似模板、跨虚拟列表行为记录、结构化关键词，以及 AI 学习
  规则的过期续期和决定性误判自动停用。
- [ ] 在已登录 X 的真实回复区验收数字 ID 提取；未登录 SSR 页面不提供
  `data-testid`、React fiber 或 JSON-LD，只能验证 handle 回退路径。
- [ ] 在已登录 X 的时间线验收转推放行：确认 `socialContext` 的实际文案与
  DOM 结构、转推者关系能否从 React fiber 读到，以及放行后是否仍显示原作者
  badge。未登录 SSR 页面不渲染转推卡片，只能验证纯函数层。
- [ ] 在已登录 X 的主贴、回复和时间线分别验收引用作者提取：主贴只标记不隐藏，
  回复与时间线的外层作者保护必须优先；React 身份缺失或冲突时保持 fail-open。
- [ ] 在长回复区真实滚动验收 500 条页面行为记录：前一模板回复被虚拟列表回收后，
  后加载的近似模板仍应稳定命中，并确认没有新增闪烁或明显扫描卡顿。
- [ ] 继续减少全页 DOM 查询、MutationObserver 噪声和无效重绘，并为缓存命中率、
  长回复区滚动和连续折叠建立性能验证。
- [x] 将回复评分改为去重的帧预算队列，每帧最多处理 50 条或约 8ms；导航和作用域
  切换时清空旧任务，避免长回复串同步阻塞主线程。
- [x] 接入 BlueNoise 纯文本关键词作为低权重社区证据，拒绝执行外部任意正则，并
  覆盖来源、缓存、宽泛词误杀和组合评分回归。
- [ ] 沿着 `computeReplyBehaviorSignals` 的做法，把其余判定逻辑与 DOM 读取分离，
  让每一块都能脱离浏览器回归。
- [x] 整理 GitHub 仓库结构，至少包含 userscript、示例名单、精简 README、LICENSE、
  CHANGELOG 和必要文档。
- [x] README 只保留安装、核心能力、隐私权限、配置、已知限制和贡献方式；删除开发期
  排查细节及重复说明。
- [x] 用 CHANGELOG 或 `docs/history.md` 保留项目从基础关键词过滤，到名单订阅、
  误封保护、连续折叠、账号标记、缓存、性能优化和可选 AI 学习的升级过程。
- [x] 发布前检查仓库中不包含 API Key、Cookie、本地路径、私人放行名单、浏览记录或
  未经核实的个人数据。
- [x] 创建公开 GitHub 仓库并在 `main` 提供可直接安装及自动更新的 `.user.js`。
- [ ] 添加版本标签与 GitHub Release。

## 顺序

1. 冻结当前功能行为并补测试。
2. 重构和性能验证。
3. 整理公开文档及项目演进记录。
4. 安全检查后发布 GitHub。
