{
  "name": "purify-x",
  "version": "2.8.0",
  "private": true,
  "description": "净化 X/Twitter 回复区的引流、诈骗与批量垃圾内容的油猴脚本",
  "license": "MIT",
  "scripts": {
    "build": "node scripts/build-userscript.mjs",
    "build:check": "node scripts/build-userscript.mjs --check",
    "test": "npm run build:check && node --test test/*.test.mjs",
    "check": "npm run build:check && node --check purify-x.user.js"
  }
}
