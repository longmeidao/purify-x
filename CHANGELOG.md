# Changelog

## 2.7.7

- 缓存重挂载与正常扫描共用详情页主贴及主贴作者续写放行边界，避免身份信息晚到时
  短暂恢复旧隐藏状态。
- 归一化移除完整 Unicode `Default_Ignorable_Code_Point` 字符类，阻止变体选择器、
  软连字符和不可见运算符插入规则文本规避判定。
- 详情页作者解析收紧为标准 `/<handle>/status/<id>` 路径，并补齐路径、评分、缓存与
  文档版本一致性回归。
- 关键词匹配改为每条推文只归一化正文和昵称一次，并缓存结构化规则；纯字面词先经
  分块 trie 快速排除无命中内容，命中后仍按原顺序输出可解释证据。
- Twitter Block Porn、TweetGuard 和自定义订阅保存 ETag 并支持 304；未更新时保留
  原数据与更新时间，不重新解析、全页刷新或清空判定缓存。
- 隐藏占位行增加“加入本地屏蔽”，将作者 handle 持久写入个人名单但不调用 X 接口；
  与永久放行共用事务保存，失败时完整回滚本地状态。
- MutationObserver 将同一轮变化归并到受影响的顶层 tweet，只在导航、Media/Profile
  控件或新增跨账号行为证据时扩大扫描范围，减少滚动时反复全页查询和评分。
- 从 x-comment-blocker 词库提取“是这个吧，看了就知道了 + t.cn 短链”高置信模板；
  同时加入无短链正常对照，避免把普通询问句升级为强规则。

## 2.7.6

- 详情页主贴保护从「只比 statusId」改为「statusId 或作者 handle」双重判定；
  正文卡片的 statusId 提取异常时，按 URL 里的作者 handle 兜底放行，
  命中 MXGA 等公开名单也不能隐藏用户主动打开的主贴。
- 详情页里主贴作者自己的续写回复同样放行（只显示名单 badge，不隐藏）；
  其他账号的回复仍进入完整过滤。

## 2.7.5

- 修复账号主页 Posts/Replies 被旧扫描入口提前跳过，导致推广过滤未实际运行的问题。
- 账号主页现在执行独立的推广内容开关；单条 `/status/...` 详情页主贴仍保持可见。
- 增加扫描入口回归，避免作用域函数正确但主扫描提前返回的假覆盖。
- handle 旁的长标志简化为“低”“广”“疑”“放”，完整含义保留在悬浮说明与
  无障碍名称中；回复占位行的操作按钮保持完整文字。

## 2.7.4

- 增加“限制回复 + Telegram 外链”的高置信引流组合，不再要求额外推广词或暗号。
- 开放回复的 Telegram 帖仍须命中明确推广话术；普通外链维持原三联条件。
- 增加 `@ac520_like` 真实 React 回复策略及漏判文本的回归覆盖。

## 2.7.3

- 开放回复的帖子只要同时命中 Telegram 外链与明确推广话术，也进入高置信推广过滤。
- 普通外链仍须与限制回复和推广话术同时出现，保留原有误杀边界。
- 增加 `@SerenaMotolaYe` 实际漏判样本及对应原因回归。

## 2.7.2

- 拆分“屏蔽时间线中的可疑账号内容”和“屏蔽时间线中的推广内容”两个选项。
- 可疑账号内容默认关闭；高置信推广内容默认开启，并覆盖账号主页帖子列表。
- 识别不用付费、今天进群、视频完整版、免费开放和进裙入口等推广变体。

## 2.7.1

- 增加 GitHub 自动更新、下载、主页和问题反馈元数据。
- 建立公开安装入口与发布文档。

## 2.7.0

- 识别“限制回复 + 外部链接 + 推广话术”的高置信推广组合。
- 允许该组合覆盖明确已关注保护；自己、关系未知、永久放行和已关注转推继续放行。
- 将占位条的永久放行按钮移到最右并降低视觉强调。

## 2.6.0–2.6.5

- 分离页面作用域与证据评分，恢复详情页主贴始终可见。
- 增加引用作者证据、CJK 近似模板、跨虚拟列表记录和 AI 学习规则护栏。
- 修复多图轮播宽度、返回位置恢复、MXGA 申诉开关及 Media 默认 Photos。

更早版本的完整演进记录见 [安装说明](docs/Purify-X-安装说明.md)。
