# Purify X

油猴脚本，净化 X/Twitter 回复区及可选时间线的引流、诈骗与批量垃圾。当前 **v2.7.5**。

> 本项目原本散落在 Codex 临时工作目录，2026-07 迁移到此处成为正式项目。
> 原目录保留未动。
> 历史会话存档在 `history/`（已加入 .gitignore）。

## 结构

| 文件 | 说明 |
|---|---|
| `purify-x.user.js` | 主脚本，单文件 ~216KB |
| `purify-x-local-list.example.json` | 本地名单示例 |
| `test/` | 判定回归测试，`npm test` 运行（无第三方依赖） |
| `package.json` | 只有 `test` / `check` 两个脚本，不引入运行时依赖 |
| `docs/Purify-X-安装说明.md` | 安装与配置文档 |
| `TODO.md` | 待办（rebrand → 重构 → 发布） |
| `AGENTS.md` | Codex 等自动化代理的项目级工作约束 |

## 核心约束

- **单文件 userscript**，不能拆成模块（油猴限制）
- 已关注账号通常放行；仅当帖子出现“Telegram 外链 + 推广话术”，或同时出现
  “限制回复 + 普通外链 + 推广话术”，或出现“限制回复 + Telegram 外链”时进入
  评分。当前账号自己、关系未知和永久放行仍是硬保护；转推主体仍是**转推者**
- 自动同步公开名单，来源 `x.zuoluo.tv` 和 `raw.githubusercontent.com`
- 依赖 `GM_getValue` / `GM_setValue` / `GM_xmlhttpRequest` / `GM_registerMenuCommand`

## 改动时注意

- **改任何判定逻辑前，先建立当前行为的回归样本** —— 重构不能改变判定结果。
  已有基线在 `test/`，改规则后必须跑 `npm test`；正常样本的 `maxScore` 被顶破时
  要先审视规则边界，不要直接抬高上限
- 脚本在没有 `document` 的环境下会跳过 bootstrap 并暴露 `__PURIFY_X__`，
  测试因此可以直接加载发布文件，不用复制规则或搭 DOM
- 每条屏蔽都要保留可解释的原因，不要让评分变成黑箱
- 关键词与正则有重复计分的风险，改规则时审计字段边界
- 性能敏感：减少全页 DOM 查询、MutationObserver 噪声、无效重绘
- **改品牌名时必须保留现有 GM 存储键**，否则用户的个人名单、订阅、AI 设置、
  判定缓存会在升级后丢失

## 发布前检查

仓库中不得包含 API Key、Cookie、本地路径、私人放行名单、浏览记录或未核实的个人数据。

## 待办顺序（见 TODO.md）

1. 冻结当前功能行为并补测试（已有第一批基线，仍需覆盖存储/名单/缓存/DOM）
2. 重构和性能验证
3. 整理公开文档及演进记录
4. 安全检查后发布 GitHub

## 状态

当前目录已建立 Git 仓库，以 **v1.14.0** 作为首个代码与文档基线，并在其上完成
**Purify X v2.0.0** 品牌迁移。v2.1.0 补上第一批判定回归测试，并新增跨账号模板
复用与句内 emoji 插入两项特征；v2.2.0 让 MXGA 名单优先按不可变数字 user ID
匹配，并保留 handle 回退与冲突放行。v2.4.0 默认关闭时间线屏蔽，回复区继续过滤；
v2.5.0 允许详情页主贴按账号名单过滤，不再无条件放行主贴卡片。
v2.6.0 把页面作用域与证据评分分离，补充引用作者名单判定、CJK 近似模板、
跨虚拟列表行为记录、结构化自定义关键词，以及 AI 学习规则过期与误判停用。
v2.6.1 兼容 X 在 Zen/Firefox 中偶发漏加满宽 class 的多图轮播，只修复已塌缩
且父容器宽度正常的多图节点。v2.6.2 保留按页面隔离的预隐藏缓存，并在从列表
返回时按原推文的视口位置纠正 X 偶发失效的滚动恢复。v2.6.3 增加默认开启的
MXGA 申诉按钮可见性开关，关闭只隐藏申诉入口。v2.6.4 恢复详情页主贴始终可见，
并在进入账号 Media 页时优先选择 Photos；用户仍可手动切换 Videos。v2.6.5 适配
X 把 Photos 收进 Videos 下拉菜单的新结构，改为先展开菜单、再选择 Photos。
v2.7.0 识别“限制回复 + 正文外链 + 粉丝福利/推广话术”的高置信广告组合，即使作者
已关注也可过滤；永久放行保持最高优先级，占位按钮移到最右并简称“永久放行”。
v2.7.1 建立公开 GitHub 更新源，userscript 管理器按 `@version` 自动检查和覆盖更新。
v2.7.2 拆分时间线“可疑账号内容”和“推广内容”开关：前者默认关闭，后者默认开启；
并补充不用付费、今天进群、视频完整版、免费开放和进裙入口等推广变体。
v2.7.3 允许“Telegram 外链 + 明确推广话术”在开放回复时进入高置信推广过滤；
普通外链仍必须同时命中限制回复，避免扩大误杀。
v2.7.4 增加“限制回复 + Telegram 外链”组合，不再要求额外推广词或暗号，并补充
React 候选读取路径。
v2.7.5 修复账号主页 Posts/Replies 被旧扫描入口提前跳过的问题，使高置信推广在
个人主页也实际进入过滤；单条详情页主贴继续保留。handle 旁的长标志同步压缩为
“低”“广”“疑”“放”，完整说明保留在 title 与 aria-label；回复占位操作不缩写。
后续功能、重构、文档和发布准备应按意图拆分提交；
公开仓库为 `https://github.com/longmeidao/purify-x`；正式发布仍需添加版本标签与
GitHub Release。
